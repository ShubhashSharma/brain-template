# Projects

Internal initiatives. Same shape as `clients/` — MOC, context, sessions, tasks, lessons — but for things your business owns rather than client work.

Examples:
- A product you're building
- A side venture
- A research project
- An infrastructure migration

Use `_templates/project-MOC-template.md`.
